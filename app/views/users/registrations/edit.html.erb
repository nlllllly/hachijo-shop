<div class="mx-5">
  <div class="row">

    <div class="col-lg-8 my-4 p-4 mx-auto" style="background-color: #EAEAEA; border: 3px solid #272343; border-radius: 5px;">

      <h3>アカウント情報の編集</h3>
      <div class="pt-3"></div>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <div class="form-row m-2">
          <div class="field form-group col-6">
            <%= f.label :last_name_roman, "* 姓（カタカナ）", class:"col-12" %>
            <%= f.text_field :last_name_roman, autocomplete: "last_name_roman", placeholder:"ヤマダ", class:"col-lg-10" %>
            <div class="mb-3"></div>
          </div>
          <div class="field form-group col-6">
            <%= f.label :first_name_roman, "* 名（カタカナ）", class:"col-12" %>
            <%= f.text_field :first_name_roman, autocomplete: "first_name_roman", placeholder:"タロウ", class:"col-lg-10" %>
            <div class="mb-3"></div>
          </div>


          <div class="field form-group col-6">
            <%= f.label :last_name, "* 姓（漢字）", class:"col-12" %>
            <%= f.text_field :last_name, autocomplete: "last_name", placeholder:"山田", class:"col-lg-10" %>
            <div class="mb-3"></div>
          </div>
          <div class="field form-group col-6">
            <%= f.label :first_name, "* 名（漢字）", class:"col-12" %>
            <%= f.text_field :first_name, autocomplete: "first_name", placeholder:"太郎", class:"col-lg-10" %>
            <div class="mb-3"></div>
          </div>

          <div class="field form-group col-6">
            <%= f.label :sex, "* 性別" , class:"col-12" %>
            <%= f.select :sex, ['男性', '女性'], {include_blank: '選択してください'}, autocomplete: "sex", class:"col-lg-10" %>
            <div class="mb-3"></div>
          </div>

          <div class="field form-group col-6">
            <%= f.label :birth_date, "* 生年月日", class:"col-12" %>
            <%= f.date_select :birth_date, use_month_numbers: true, start_year: 1930, end_year: (Time.now.year - 0), default: Date.new(1990, 1, 1), class:"col-lg-6" %>
            <div class="mb-3"></div>
          </div>

          <div class="field form-group col-12">
            <%= f.label :email, "*メールアドレス", class:"col-12" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"col-md-4" %>
            <div class="mb-3"></div>
          </div>

          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
          <% end %>
          
          <div class="field form-group col-12">
            <%= f.label :current_password, "* 現在のパスワード", class:"col-12" %>
            <%= f.password_field :current_password, autocomplete: "current-password", class:"col-md-4" %>
            <p>※変更を保存する場合は、現在のパスワードを入力してください。</p>
            <div class="mb-3"></div>
          </div>

          <div class="border-top border-info col-12" style="padding: 20px;"></div>

          <div class="field form-group col-12">
            <%= f.label :password, "* 新しいパスワード", class:"col-12" %>
            <%= f.password_field :password, autocomplete: "new-password", class:"col-md-4" %>
            <% if @minimum_password_length %>
              <p>※パスワードは<%= @minimum_password_length %> 文字以上で、変更する際のみ入力してください。
            <% end %>
            <div class="mb-3"></div>
          </div>

          <div class="field form-group col-12">
            <%= f.label :password_confirmation, "* 新しいパスワードの確認", class:"col-12" %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"col-md-4" %>
            <div class="mb-3"></div>
          </div>
          
          <%# エラーメッセージ %>
          <%= render "users/shared/error_messages", resource: resource %>

          <div class="actions form-group col-12 text-center">
            <div class="mt-3"></div>
            <%= f.submit "変更を保存する", class:"btn btn-info col-lg-9" %>
          </div>
        </div>

      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="col-lg-8 my-4 p-4 mx-auto" style="background-color: #EAEAEA; border: 3px solid #272343; border-radius: 5px;">
      <h3>アカウントの削除</h3>
      <div class="pt-3"></div>
      <p>※ アカウントを削除する場合はこちらから
      <div class="actions form-group col-12 text-center">
        <%= button_to "アカウントを削除する", registration_path(resource_name), class:"btn btn-danger col-lg-9", data: { confirm: "本当に削除してもいいですか？" }, method: :delete %>
      </div>
    </div>
  </div>

  <div class="row p-3">
    <%= link_to "戻る", user_path(@user.id) %>
  </div>
    
</div>
